
// let chapterNum = parseInt(document.querySelector("h2.ltx_title_section").firstElementChild.textContent);
// if (chapterNum >= 24) ++chapterNum;

// for (const [i, pre] of document.querySelectorAll("pre.ltx_verbatim").entries()) {
// 	const url = `https://venhance.github.io/napkin/figures/Napkin-${chapterNum}${String.fromCodePoint(0x41 + i)}.pdf`;
// 	const objectElement = document.createElement("object");
// 	objectElement.data = `${url}#toolbar=0&navpanes=0&scrollbar=0&view=Fit`;
// 	objectElement.type = "application/pdf";
// 	pre.replaceWith(objectElement);
// }
